// Run docker build

pipeline {
    agent any
    stages {
          stage("create docker image") {
            steps {
                echo " ============== start building image =================="
                dir ('Docker/toolbox') {
                	sh 'docker build . '
                }
            }
        }
    }
}