// Run docker build

pipeline {
    agent any
    triggers { pollSCM('* * * * *') }
    stages {
          stage("create docker image") {
            steps {
                echo " ============== start building image =================="
                dir ('Docker/toolbox') {
                	sh 'docker build . '
                echo "=============== end building image ===================="  
                }
            }
        }
    }
}